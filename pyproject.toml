[project]
name = "mech-agent"
version = "0.1.0"
description = "OpenHands plugin for conversational mechanical design (enclosures, mechanisms, brackets)"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2",
    "build123d==0.13.0",
    "mcp>=1.29,<2",
    "openhands-sdk==1.49.4",
    "openhands-tools==1.49.4",
]

[dependency-groups]
dev = [
    "packaging>=26",
    "pyright>=1.1.414",
    "pytest>=9",
    "pytest-xdist>=3",
    "ruff>=0.16",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mech"]

[tool.uv]
required-version = "==0.12.18"
# threejs-materials<1.3 (required by build123d>=0.13) caps pillow<12.3,
# which conflicts with openhands-sdk>=1.49.4's pillow>=12.3. threejs-materials
# feeds build123d's viewer/material export only, so overriding the pillow
# bound is safe for the mech generation/gate path.
override-dependencies = ["pillow>=12.3.0,<13"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "scripts", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "strict"
include = ["src", "scripts", "tests"]
exclude = ["**/__pycache__", ".venv"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q -n auto --dist loadgroup"
pythonpath = ["."]
markers = ["slow: tests that build full CAD geometry"]
